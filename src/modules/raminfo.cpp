#include "modules.hpp"

#include <string>
#include <vector>

static bool raminfo_module(const char *argv[], int argc)
{
    int found = 0;
    return found;
}

REGISTER_MODULE_HANDLER(raminfo_module);